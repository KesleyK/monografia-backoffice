import { Box } from "@mui/material";
import { ResponsiveDrawer } from "../../components";

export function Home() {
    return (
        <Box>
            <ResponsiveDrawer>
                <span>Oiw</span>
            </ResponsiveDrawer>
        </Box>
    );
}
