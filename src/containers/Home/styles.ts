const styles = {
    container: {
        padding: 5
    },
    card: {
        height: 200,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column"
    },
    gridContainer: {
        marginBottom: 10
    }
};

export default styles;
