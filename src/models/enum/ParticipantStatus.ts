export enum ParticipantStatus {
    PENDING = "pending",
    ACCEPTED = "accepted",
    DISABLED = "disabled"
}

export enum TranslatedParticipantStatus {
    PENDING = "pendente",
    ACCEPTED = "aceito",
    DISABLED = "desativado"
}
